QWEN_API_KEY=your_qwen_api_key_here
QWEN_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1
MONGODB_URI=mongodb://localhost:27017/
MILVUS_URI=http://localhost:19530
EMBEDDING_MODEL=text-embedding-v3-small
# If using DashScope embeddings or another provider, adjust accordingly.
# For this example, I'll use OpenAI compatible embeddings or HuggingFace local embeddings.
# Let's stick to HuggingFace local for embeddings to be safe and free, or ask the user.
# Given the user asked for Qwen, they might want Qwen embeddings too.
# I will use a generic "OpenAI" style client for Qwen.
